server:
  port: 8080
  servlet:
    context-path: /user

spring:
  datasource:
    url: jdbc:mysql://localhost/plazoleta
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update

springdoc:
  swagger-ui:
    enabled: true
    persistAuthorization: true

appDescription: "User API"
appVersion: "1.0.0"